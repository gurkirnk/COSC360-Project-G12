This is our project for Cosc 360

start it by running `docker compose up -d`
To ensure you are running it with the most recent version of the frontend and backend, ensure you added `--build`

Default initial admin credentials are 'admin@admin.com' and 'apassword', they can and should be changed in the .env file

Proof of testing can be found in 'Documentation'
